# Dashboard — design

**Primary question:** What is the health of the project and what needs attention?

**Main artefacts:** Aggregates — open/handled problems, unhandled/approved specifications, open/completed tasks.

**Depends on (conceptual):** overview rollups from API; numbers must align with default filters in other views ([shared/specification.md](../shared/specification.md) C5).

**Outgoing navigation:** Problem, Specification, Task (and Architecture when domain work is the bottleneck) — prefer deep links with unhandled-only filters when supported.

Index: [../README.md](../README.md).
