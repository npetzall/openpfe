# serde — lock-update

Resolution-only preview script failed (trial manifest path). After `cargo test -p openpfe-ipc`, lock adds `serde` / `serde_derive` as direct resolution for `openpfe-ipc` (likely already transitive via `serde_json`).
